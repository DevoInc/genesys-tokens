{
  "name": "@devoinc/dali-brand-devo",
  "version": "1.0.3",
  "author": "Devo Dev Team",
  "license": "MIT",
  "description": "Devo brand tokens",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "clean": "rm -rf dist/light dist/dark",
    "build": "npm run build:light && npm run build:dark",
    "build:light": "dit build -i \"schemes/light/*.json\" -o dist/light",
    "build:dark": "dit build -i \"schemes/dark/*.json\" -o dist/dark",
    "validate": "npm run validate:light && npm run validate:dark",
    "validate:light": "dit validate dist/light/",
    "validate:dark": "dit validate dist/dark/",
    "dist": "npm run clean && npm run build && npm run validate"
  },
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git@github.com:DevoInc/dali-tokens.git"
  },
  "dependencies": {
    "@devoinc/dali-tokens-cli": "^1.0.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
